/*
def virouMes():
    dia = time.strftime('%d', time.localtime())
    dia_virada = str(bd.retorna_ajustes()[0][2]).zfill(2)
    if(dia == dia_virada):
        bd.altera_divida()
    return


def zeraFaltas():
    dia = time.strftime('%d', time.localtime())
    dia_virada = str(bd.retorna_ajustes()[0][2]).zfill(2)
    if(dia == dia_virada):
        bd.zera_faltas()
    return
    */

